from setuptools import setup, find_packages

setup(
    name='cognitivepersonalizationtaskfingerprinting',
    version='1.0.1',
    packages=find_packages(),
    install_requires=[
    ],
    entry_points={
        'console_scripts': [        ],
    },
    # Add additional metadata as needed
    author='Dennis Paulino',
    description='The source code is published in the following repository: https://github.com/dennispaulino/TaskFingerprintingForCognitivePersonalizationOfWebTasksDesignPackage',
)

