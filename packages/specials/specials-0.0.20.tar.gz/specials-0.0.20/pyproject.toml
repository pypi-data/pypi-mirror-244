[tool.poetry]
name = "specials"
version = "0.0.20"
description = ""
authors = ["Mika Hänninen <mika.hanninen@gmail.com>"]
license = "MIT"
readme = "README.md"

packages = [
	{ include = "SapGuiLibrary", from = "vendor" },
	{ include = "docx", from = "vendor" },
	{ include = "fire", from = "vendor" },
	{ include = "pyperclip", from = "vendor" },
	{ include = "stringcase", from = "vendor" },
	{ include = "validators", from = "vendor" },
	{ include = "htmldocx", from = "vendor" },
	{ include = "pendulum", from = "vendor" },
]

[tool.poetry.dependencies]
python = "^3.8.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"