[tool.poetry]
name = "django_health_check_prometheus"
version = "0.1.0"
description = "This is a prometheus adapter for django health check"
authors = ["MrLYC <fimyikong@gmai.com>"]
readme = "README.md"
packages = [{ include = "django_health_check_prometheus" }]

[tool.poetry.dependencies]
python = ">=3.6"
django-health-check = ">=3.16.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
