from setuptools import setup, find_packages

setup(
    name='lazycodet-nlp',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Các dependencies nếu có
    ],
)
