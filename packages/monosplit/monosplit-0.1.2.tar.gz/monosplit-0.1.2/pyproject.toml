[project]
name = "monosplit"
version = "0.1.2"
description = ""
authors = [{"name" = "Grayson Miller", "email" = "grayson.miller124@gmail.com"}]
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages]
find = {}

[project.scripts]
monosplit = "monosplit.__main__:main"
