[build-system]
requires = [
	"setuptools>=42",
	"wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "fprlib"
version = "0.0.1"
authors = [
  { name="DrKvass"},
]
description = "Knjižnica za lažje opravljanje FPR na FMFju"

requires-python = ">=3.8"