[tool.poetry]
name = "databricks-sqlalchemy-oauth"
version = "0.1.0"
description = "SQLAlchemy OAuth connector to Databricks"
authors = ["Simona Barankova"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
sqlalchemy = "^2.0.0"
databricks-sql-connector = "^3.0.0"
databricks-sdk = "^0.13.0"
pydantic = "^2.5.2"


[tool.poetry.group.dev.dependencies]
pytest = "^7.4.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
