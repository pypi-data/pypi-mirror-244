sources = files('__init__.py')

python.install_sources(
    sources,
    subdir: 'calliope/subprojects')

calliope_module_sources += sources

# The bundled projects themselves are installed from /subprojects.
