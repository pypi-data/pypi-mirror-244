from setuptools import setup, find_packages

setup(
    name='cogpersonalization-taskfingerprinting',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
    ],
    entry_points={
        'console_scripts': [

        ],
    },
    # Add additional metadata as needed
    author='Dennis Paulino',
    description='Your package description',
)

