[tool.poetry]
name = "fern-cohere-aws"
version = "0.0.7"
description = ""
readme = "README.md"
authors = []
packages = [
    { include = "cohere_aws", from = "src"}
]

[tool.poetry.dependencies]
python = "^3.7"
boto3 = ">=1.28.57"
botocore = ">=1.31.57"
httpx = ">=0.21.2"
pydantic = ">= 1.9.2, < 2.5.0"

[tool.poetry.dev-dependencies]
mypy = "0.971"
pytest = "^7.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
